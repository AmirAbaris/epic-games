import { IsHomeCardActionPipe } from './is-home-card-action.pipe';

describe('IsHomeCardActionPipe', () => {
  it('create an instance', () => {
    const pipe = new IsHomeCardActionPipe();
    expect(pipe).toBeTruthy();
  });
});
