import { ActivateWishlistDirective } from './activate-wishlist.directive';

describe('ActivateWishlistDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivateWishlistDirective();
    expect(directive).toBeTruthy();
  });
});
