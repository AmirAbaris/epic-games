import { IsHighlightPreviewPipe } from './is-highlight-preview.pipe';

describe('IsHighlightPreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new IsHighlightPreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
