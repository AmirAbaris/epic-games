import { TruncateLinePipe } from './truncate-line.pipe';

describe('TruncateLinePipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateLinePipe();
    expect(pipe).toBeTruthy();
  });
});
