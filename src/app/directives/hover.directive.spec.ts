import {TitleHoverDirective} from './title-hover.directive';

describe('HoverDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleHoverDirective();
    expect(directive).toBeTruthy();
  });
});
