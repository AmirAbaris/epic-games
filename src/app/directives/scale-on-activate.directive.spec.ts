import { ScaleOnActivateDirective } from './scale-on-activate.directive';

describe('ScaleOnActivateDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaleOnActivateDirective();
    expect(directive).toBeTruthy();
  });
});
