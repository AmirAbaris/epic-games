import { TextHoverIndicatorDirective } from './text-hover-indicator.directive';

describe('TextHoverIndicatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TextHoverIndicatorDirective();
    expect(directive).toBeTruthy();
  });
});
